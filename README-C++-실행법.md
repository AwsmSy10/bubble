# C++ 버전 실행법

`bubble_arena.cpp`는 **외부 라이브러리 없는 C++ Windows 게임 소스**입니다. raylib가 필요 없습니다.

Visual Studio에서 **빈 C++ 프로젝트**를 만든 뒤, 프로젝트에 `bubble_arena.cpp` 파일을 추가하고 `F5`를 누르면 실행됩니다. 설치할 때 **Desktop development with C++**만 선택하면 됩니다.

조작은 `WASD` 또는 방향키로 이동, `Space`로 버블 설치, `R`로 다시 시작입니다.
